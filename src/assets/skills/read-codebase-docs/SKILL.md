---
name: read-codebase-docs
description: Read documents to understand the codebase. Use when a new session starts.
---

You should read all documents under `docs` directory that were created via `read-through-codebase` skill. If those documents do not exist, output a message indicating their absence, but do not trigger `read-through-codebase` skill.
